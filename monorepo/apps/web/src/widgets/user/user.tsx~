'use client'

import { PropsWithUser, userFullName } from '@stroy/models'

import { Group, PageTitle } from '~packages/ui'

export default function User({ user }: PropsWithUser) {
	return (
		<Group>
			<PageTitle title={userFullName(user)} />
		</Group>
	)
}
